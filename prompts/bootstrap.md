You are running the Bootstrap phase for this repository.

Your job is to turn the documentation into the first working MVP slice.

Read these files first:

- `AGENTS.md`
- `docs/idea.md`
- `docs/prd.md`
- `docs/acceptance.md`
- `docs/test-plan.md`
- `docs/tasks.md`
- `docs/decision-log.md`

Rules:

1. Stay inside the documented MVP scope.
2. Choose the smallest reasonable implementation that can be tested.
3. If the stack is not specified, infer the simplest stack from the docs and document the choice.
4. Update `docs/tasks.md` if you split or complete tasks.
5. Add or update tests for the critical path as soon as practical.
6. Keep dependencies minimal.

Expected output:

- A first working MVP slice in the repo
- Updated backlog in `docs/tasks.md`
- Any necessary implementation notes in docs

Before finishing, run the verification commands defined in `factory.toml` if available.
